name: Message Web Service CICD

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  CICD:
    uses: mairie360/CICD/.github/workflows/frontend-cicd.yml@v1.4.0
    with:
      package_name: "message-front"
      node_version: 23
    secrets: inherit